# Credit (Luhn Algorithm) - C

Validates credit card numbers using Luhn’s algorithm and identifies card brand.

## Compile
gcc -std=c17 -Wall -Wextra -pedantic credit.c -o credit.exe

## Run
./credit.exe